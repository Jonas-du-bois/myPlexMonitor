# ═══════════════════════════════════════════════════════════════════════════════
# ║                        MyPlexMonitor Configuration                           ║
# ═══════════════════════════════════════════════════════════════════════════════

# --- Telegram Bot Configuration ---
# Get your bot token from @BotFather on Telegram
TELEGRAM_TOKEN=your_telegram_bot_token

# Your Telegram Chat ID for automatic alerts
# Use /start command to see your Chat ID
TELEGRAM_CHAT_ID=your_telegram_chat_id

# --- Server Configuration ---
# Your server IP address (Plex + qBittorrent run on the same machine)
SERVER_IP=your_server_ip_address

# Plex server port (default: 32400)
PLEX_PORT=32400

# Your Plex authentication token
# Find it at: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
PLEX_TOKEN=your_plex_auth_token

# --- Monitoring Configuration ---
# Server check interval in milliseconds (default: 30000 = 30 seconds)
CHECK_INTERVAL=30000

# Download check interval in milliseconds (default: 60000 = 1 minute)
DOWNLOAD_CHECK_INTERVAL=60000

# --- qBittorrent Configuration ---
# qBittorrent WebUI port (default: 8080)
# Note: Uses SERVER_IP by default (same machine as Plex)
QBITTORRENT_PORT=8080

# qBittorrent WebUI username
QBITTORRENT_USERNAME=admin

# qBittorrent WebUI password
QBITTORRENT_PASSWORD=adminadmin

# --- Download Paths Configuration ---
# Path where movies will be downloaded
MOVIES_PATH=/mnt/storage/plex/films

# Path where series/TV shows will be downloaded
SERIES_PATH=/mnt/storage/plex/series

# --- Security Configuration ---
# Comma-separated list of Telegram User IDs that are allowed to use this bot
# Leave empty to allow all users (NOT recommended for production)
# Example: AUTHORIZED_USERS=123456789,987654321
AUTHORIZED_USERS=

# --- Web Server Configuration ---
# Port for the web server (used by hosting platforms like Render)
PORT=3000

# Webhook URL for production deployment (Render)
# IMPORTANT: Set this to avoid Telegram 409 Conflict errors
# Example: WEBHOOK_URL=https://myplexmonitor.onrender.com
WEBHOOK_URL=